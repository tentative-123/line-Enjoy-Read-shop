'use client'
import { useEffect,useState } from 'react'
const API=process.env.NEXT_PUBLIC_API_URL!
export default function CafeBookings(){const [q,setQ]=useState('');const [rows,setRows]=useState<any[]>([]);const load=()=>fetch(`${API}/api/admin/cafe/bookings?q=${encodeURIComponent(q)}`,{credentials:'include'}).then(r=>r.json()).then(r=>r.success&&setRows(r.data));useEffect(()=>{ void load() },[]);return <main className="p-6 space-y-4"><h1 className="text-2xl font-bold">無人咖啡廳預約管理</h1><div className="flex gap-2"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="姓名、手機或預約編號" className="border rounded-lg px-3 py-2 w-80"/><button onClick={load} className="bg-emerald-700 text-white rounded-lg px-4">搜尋</button></div><div className="bg-white rounded-xl overflow-auto"><table className="w-full text-sm"><thead><tr className="text-left border-b">{['預約編號','使用者／LINE','手機','空間','日期時間','人數','預約狀態','付款','入場','建立時間'].map(x=><th key={x} className="p-3">{x}</th>)}</tr></thead><tbody>{rows.map(b=><tr key={b.id} className="border-b"><td className="p-3 font-mono">{b.bookingCode}</td><td>{b.customerName}<br/><span className="text-gray-500">{b.lineDisplayName}</span></td><td>{b.customerPhone}</td><td>{b.spaceName}</td><td>{new Date(b.startsAt).toLocaleString('zh-TW',{timeZone:'Asia/Taipei'})}<br/>～ {new Date(b.endsAt).toLocaleTimeString('zh-TW',{timeZone:'Asia/Taipei'})}</td><td>{b.quantity}</td><td>{b.status}</td><td>{b.paymentStatus}</td><td>{b.checkedInAt?'已入場':'未入場'}</td><td>{new Date(b.createdAt).toLocaleString('zh-TW',{timeZone:'Asia/Taipei'})}</td></tr>)}</tbody></table></div></main>}
